#include <iostream>
#include <cstring>

using namespace std;

int main() {

    int n;
    string a, b;

    cin >> n;

    while(n--) {
        cin >> a >> b;

        int tam_a = a.size();
        char a_array[tam_a];
        strcpy(a_array, a.c_str());

        int tam_b = b.size();
        char b_array[tam_b];
        strcpy(b_array, b.c_str());

        for(int i = )


    }



    return 0;
}
