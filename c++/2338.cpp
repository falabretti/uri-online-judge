#include <iostream>
#include <cstring>
#include <cstdio>
using namespace std;
int main() {
    int n;
    cin >> n;
    while(n--) {

        //entrada da mensagem
        string entrada;
        cin >> entrada;

        int contador = 0;

        //copia string para char array
        int tam = entrada.size();
        char entrada_array[tam];
        strcpy(entrada_array, entrada.c_str());

        for(int = 0; i < tam; i++) {
            if(entrada_array[i] == '=') {

            }
        }









    }





    return 0;
}
