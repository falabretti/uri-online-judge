#include <cstdio>

int main() {

    //freopen("2776.in", "r", stdin);
    int promo, latas, teste, maior;


    while(scanf("%d", &promo) != EOF) {
        scanf("%d", &latas);

        int quantidade[promo], valor[promo];

        for(int i = 0; i < promo; i++) {
            scanf("%d %d\n", &quantidade[i], &valor[i]);
        }
    }

    return 0;
}
